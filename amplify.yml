applications:
  - appRoot: frontend
    env:
      HUSKY: 0
    frontend:
      phases:
        preBuild:
          commands:
            - npm ci --workspace=frontend --cache .npm --prefer-offline
        build:
          commands:
            - npm run build
          env:
            CI: true
      artifacts:
        baseDirectory: dist
        files:
          - '**/*'
      cache:
        paths:
          - '.npm/**/*'
    buildPath: /
