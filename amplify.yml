applications:
  - appRoot: frontend
    frontend:
      phases:
        preBuild:
          commands:
            - npm ci --workspace=frontend --cache .npm --prefer-offline
          env:
            HUSKY: 0
        build:
          commands:
            - npm run build
          env:
            CI: true
      artifacts:
        baseDirectory: dist
        files:
          - '**/*'
      cache:
        paths:
          - '.npm/**/*'
    buildPath: /
