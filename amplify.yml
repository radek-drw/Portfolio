applications:
  - appRoot: frontend
    frontend:
      phases:
        preBuild:
          commands:
            - CI=true npm ci --workspace=frontend --cache .npm --prefer-offline
        build:
          commands:
            - npm run build
          env:
            CI: true
      artifacts:
        baseDirectory: dist
        files:
          - '**/*'
      cache:
        paths:
          - '.npm/**/*'
    buildPath: /
