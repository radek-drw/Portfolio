{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend application (Node.js/AWS-SDK)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test:sendContactForm": "node test-sendContactForm",
    "deploy:sendContactForm": "powershell Compress-Archive -Path sendContactForm.js -DestinationPath sendContactForm.zip; aws lambda update-function-code --function-name send-contact-form --zip-file fileb://sendContactForm.zip --no-cli-pager; Remove-Item -Path ./sendContactForm.zip"
  },
  "keywords": [],
  "author": "Radek Drweski",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-ses": "^3.879.0"
  }
}
