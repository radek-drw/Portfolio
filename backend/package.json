{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend application (Node.js/AWS-SDK)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test:sendContactForm": "node test-sendContactForm",
    "deploy:sendContactForm": "powershell Compress-Archive -Path sendContactForm.mjs -DestinationPath sendContactForm.zip; aws lambda update-function-code --function-name send-contact-form --zip-file fileb://sendContactForm.zip --no-cli-pager; Remove-Item -Path ./sendContactForm.zip"
  },
  "keywords": [],
  "author": "Radek Drweski",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@aws-sdk/client-ses": "^3.879.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.11"
  }
}
